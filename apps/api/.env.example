# API diagnostics are enabled only when NODE_ENV and this environment are production.
SENTRY_DSN_API=
SENTRY_ENVIRONMENT_API=
SENTRY_RELEASE_API=
SENTRY_PROJECT_API=
SENTRY_ORG=
# Private build secret; never expose it to application clients.
SENTRY_AUTH_TOKEN=
