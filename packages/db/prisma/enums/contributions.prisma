enum ContributionPlanInterval {
  monthly
  quarterly
  yearly
}

enum ContributionStatus {
  pending
  posted
  failed
  reversed
}

enum ContributionChannel {
  payroll
  transfer
  cash
  manual
}

enum PaymentAllocationPreference {
  manual_split
  savings_first
  loan_first
}

enum MemberPaymentReceiptStatus {
  submitted
  under_review
  correction_requested
  approved
  rejected
}

enum MemberPaymentReceiptAllocationCategory {
  commitment
  special_savings
  loan_servicing
  loan_extra_payment
  shares
  procurement
  project_financing
  food_purchase
  other
}

enum MemberPaymentReceiptPeriodIntent {
  current_period
  future_period
  back_period
  unspecified
}

enum ChargeKind {
  fixed
  percentage
}

enum ChargeFrequency {
  recurring_monthly
  per_contribution
  one_time
  manual
}

enum ChargeValueType {
  fixed_amount
  percentage
}

enum ChargeApplicationStatus {
  pending
  posted
  waived
  reversed
}

enum ChargeWorkflow {
  commitment_collection
  loan_request
  loan
  procurement_request
  food_purchase_application
  project_financing_request
}

enum ChargeApplicabilityTrigger {
  monthly_collection
  submission
  approval
  manual
}

enum ChargeCollectionMode {
  deduct_from_savings
  pay_separately
}
