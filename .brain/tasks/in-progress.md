# In Progress

## Purpose

This file tracks work currently being executed.

## How To Use

- Keep only active items here.
- Move finished work to `done.md` with the outcome noted.

## Current In Progress

- Dashboard depth slice on the new `src/` architecture: deepen analytics, staged imports, collections queues, and reporting inside the standardized Midday-style structure without reintroducing `features/` or `primitives/`.

### Refactor Finance Tables To Midday Pattern

- Priority: Medium
- Description: Track plan in `brain/plans/2026-06-21-feature-midday-table-filter-sheet-refactor.md`.
- Related Feature: Midday Table Filter Sheet Refactor
- Status: In Progress
- Plan Status: In Progress
- Plan File: brain/plans/2026-06-21-feature-midday-table-filter-sheet-refactor.md
- Created Date: 2026-06-21
- Progress:
  Shares, charges, and business have been moved into the Midday page/header/filter/sheet/table/hook pattern.
  On 2026-07-14, `/business` also removed its remaining inline profit-history creation form in favor of the business header sheet action, then moved to the Midday invoices-style split with `open-business-sheet.tsx`, `business-sheet-header.tsx`, `business-content.tsx`, `business/form-context.tsx`, a tRPC-backed virtual table, row actions menu, column visibility, and selected-row bottom bar.
  Later on 2026-07-14, the exact Midday implementation migration started with the shared dashboard navigation foundation: route-level navigation icons now map to concrete destinations, the sidebar supports Midday-style expandable child links, and the members create flow began moving from modal launch to the URL-driven sheet/open-button/content/form-context structure used by the approved business page.
  The finance setup share and charge sheets also moved away from hand-rolled portal sheets toward the exact Midday/business sheet anatomy: URL-param open buttons plus sheet wrapper, sheet header, content router, and form-context files.
  The contributions page started the same conversion: commitment creation, payment posting, member payment preferences, and active commitment plan management now route through contribution URL params, open buttons, a sheet wrapper, a sheet header, and a content router instead of embedding those forms directly in the page body.
  The loans workspace followed the same conversion: request submission, loan request review, guarantor review, and approved-loan disbursement are now driven by loan URL params, open buttons, a sheet wrapper, a sheet header, and a content router instead of inline row/page forms.
  The operational charges page now routes charge definition creation, member charge application, waiver, and reversal through charge-operation URL params, sheet open buttons, a sheet wrapper, a sheet header, and a content router; dated charge version updates remain in the history panel for the next pass.
  Monthly records now route due-record generation, missing-month creation, member-row apply, and member-row cancel through monthly-record URL params, open buttons, a sheet wrapper, a sheet header, and a content router; the automation settings form remains inline for the next settings pass.
  Repayments now route collections status refresh, repayment posting, and collection follow-up capture through repayment URL params, open buttons, a sheet wrapper, a sheet header, and a content router instead of page/header/row inline forms.
  Member guarantor approvals now route approve/reject responses through guarantor approval URL params, open buttons, and a confirmation sheet instead of row-level submit forms.
  Cooperative profile settings now route profile editing through profile-settings URL params, an edit button, and a focused sheet instead of rendering the full profile form inline.
  Trust readiness settings now route pilot trust profile editing through trust-settings URL params, an edit button, and a focused sheet instead of rendering the full trust evidence form inline.
  Operation profile settings now route service access editing through operation-profile-settings URL params, an edit button, and a focused sheet while the page body presents the current service access summary.
  Workspace roles settings now route role assignment through role-settings URL params, an assignment button, and a focused sheet instead of rendering the role assignment form inline.
  Notifications preferences now route email preference toggles through notification-preference URL params, toggle buttons, and a confirmation sheet instead of rendering one submit form per role/type toggle.
  Later on 2026-07-14, the operational charges page finished the pending dated-version/status slice: charge activation/deactivation and dated charge updates now use charge-operation URL params and focused sheet content instead of table/history inline forms.
  Contribution collection-source batch work now follows the same page/action split: staging a source batch, posting all collected rows, marking a row collected, marking a row as an exception, and posting a single row all open through contribution URL params and sheet content instead of embedded batch forms.
  The shared finance overview/loan settings view removed stale dead charge/business branches and moved live start-date, financing policy, cycle control, and loan-product settings into a tenant-finance-settings URL-param sheet.
  Members table status changes now route through the member sheet status mode instead of row-dropdown submit forms, and monthly-record automation settings now route through the monthly-record sheet while the page body keeps only the year filter control.
  The member/import/migration cleanup pass converted the members import workspace, legacy member create shell, post-create backfill prompt, backfill history workspace, backfill adjustment controls, baseline edit surface, quick-fill workspace, and legacy loan draft editor from generic dialog shells to sheet-based surfaces. Obsolete local `DashboardTable*` adapters were renamed to neutral shadcn table helpers in migration views, and the unused legacy `components/dashboard/static-table.tsx` module was removed.
  Member signup links now present access-mode, create-link, and edit-link forms through a focused sheet workflow while cards remain summary/action surfaces; the only remaining raw dialog primitives in the dashboard scan are `AlertDialog` confirmation gates for safety-sensitive actions.
  Member backfill and initial migration high-impact actions now use focused sheet actions instead of always-visible page forms: opening balance staging/review/apply/reversal, historical share purchases, generated dividend/draft/apply actions, migration ledger draft/apply, finalization, and legacy loan review/draft creation all moved behind reusable sheet triggers while table/editor forms remain in their workflow surfaces.
  Import settings routes now share the Midday-style table runtime instead of a plain shadcn table: import batches use persisted column settings, column visibility controls, draggable/resizable headers, virtual rows, sticky import column behavior, URL-driven review/apply sheets, and empty/no-results states.
  Loan request and loan portfolio tables now use the shared Midday-style table runtime with persisted settings, column visibility, draggable/resizable headers, virtual rows, sticky member/action columns, compact row summaries, and dropdown row action menus that open the existing loan review, guarantor, and disbursement sheets.
  Membership approvals now use the shared Midday-style table runtime instead of an inline page table: the queue loads persisted table settings, exposes column visibility, uses draggable/resizable headers, virtual rows, sticky applicant/action columns, tenant-aware row navigation, and row action menus that open the existing review route.
  Notifications delivery history now uses the shared Midday-style table runtime instead of stacked history cards: the route loads persisted table settings, exposes column visibility, and renders recent delivery logs with draggable/resizable headers, virtual rows, and a sticky subject column while preserving preference sheets and summary cards.
  Procurement workflows started moving away from dense inline page forms: staff request creation, review decisions, purchase recording, and member self-service requests now open in focused sheet actions while existing cards remain summary surfaces and the existing server actions/state handling are preserved.
  Foodstuff Purchase workflows now follow the same focused sheet pattern: fund release, staff/member application entry, cycle accounting, accounting review, application review, and member self-service requests are no longer permanently embedded as page forms while existing actions and state handling remain unchanged.
  Support case workflows now use focused sheet actions for staff and member case creation, staff case updates, finance-adjustment review, and replies while the case cards remain readable status/history summaries.
  Project financing workflows now use focused sheet actions for staff request creation, member self-service requests, request review, and disbursement recording while preserving the existing review/disbursement server actions and card summaries.
  Payment receipt workflows now use focused sheet actions for staff staging, receipt review/allocation adjustment, support-case escalation, member receipt submission, and member support escalation while keeping allocation editors and existing server actions intact.
  Monthly record member rows now use the shared Midday-style table runtime instead of the local native table: the route loads persisted monthly-record table settings and sort params, the members section exposes column visibility, and the table supports sticky member columns, draggable/resizable headers, virtual rows, persisted settings, and existing row action sheets.
  Additional share applications now use focused sheet actions for request creation and pending application review instead of permanently embedding request/review controls in the share applications card.
  The operational charge library table now uses its own shared Midday-style table runtime (`chargeLibrary`) instead of the page-local native table: the charges route loads persisted charge-library settings and sort params, the definitions section exposes column visibility, and activation/deactivation still opens through the existing charge-operation sheet.
  Member detail and printable member statement read-only tables have been replaced with consistent dashboard surface-list sections for dividends, charges, and ledger transactions, removing page-local native table layouts from those detail surfaces while preserving the same displayed financial data.
  The business profit migration worksheet no longer embeds native tables for expense lines or member allocation inputs; both sections now use dashboard surface rows while preserving the same input names, local state updates, and worksheet submit action.
  The initial migration member business-profit assignment panel now uses dashboard surface rows instead of a native table while preserving the per-row migration-profit adjustment forms and save actions.
  The initial migration legacy-loan review list now uses dashboard surface rows instead of a native table while preserving the existing edit sheet trigger, lock rules, and principal-only loan draft data.
  The member ledger backfill preview no longer renders its monthly, business-profit, or loan-taken migration segments through native shadcn tables; those rows now use dashboard surface cards while preserving column visibility toggles, savings/repayment controls, status/defaulting actions, color legend, and financial values.
  Member import editing and generic import CSV preview panels no longer use local shadcn tables; both now render controlled grid surfaces while preserving column visibility/editing, sticky row labels, CSV preview values, and existing import form flows.
  Member create, member import, and post-create backfill prompt flows now live under sheet-named components instead of legacy `components/modals` paths, and the unused backfill history modal file was removed; dashboard source no longer imports `components/modals`.
  Member backfill activity-window and profit-season adjustment editors no longer use native HTML tables; both now use grid row layouts while preserving hidden fields, validation state, popovers, quick fill, add/delete controls, and form submission semantics.
  Member commitment-history and loan-history migration editors no longer use native HTML tables; both now use responsive grid rows while preserving dynamic form arrays, hidden draft fields, date/currency inputs, guarantor creation hooks, row deletion confirmation, quick fill, and redirect-on-save behavior.
  Getting-started business profit season review no longer uses native table cells; the season rows and deduction helper now render responsive grid fields while preserving season keys, tooltip entry details, draft-preserved deductions, distributable calculations, and finalization submission.
  Tenant finance setup forms no longer use native HTML table editors for start date, share history, charge definitions/history, or business/profit history; those editors now render responsive grid rows while preserving quick fill, clear, nested dated histories, applicability checkboxes, brought-forward profit status, shareable calculations, delete guards, and submit semantics.
  Member create form layout naming now uses `inSheet` instead of `inModal`, matching the sheet-based member create/import/backfill flow and removing stale modal terminology from dashboard source.
  The standalone `MemberCreateSheet` used by migration/backfill contexts now reuses the canonical member sheet form context, header, and content router instead of rendering `MemberCreateForm` directly, leaving `MemberContent` as the single create-form owner for member sheet flows.
  Member signup link access/create/edit sheets now use URL-backed `nuqs` params (`signupLinkSheetType`, `signupLinkId`) instead of local component sheet state, aligning that settings workflow with the Midday-style URL sheet contract while preserving existing access, create, edit, copy, rotate, and toggle actions.
  Member signup links now use the fuller Midday sheet split: the manager keeps page cards and URL-param open actions, while `member-signup-link-sheet.tsx`, `member-signup-link-sheet-header.tsx`, and `member-signup-link-content.tsx` own the sheet shell, header, and access/create/edit form router.
  Member import sheet no longer carries an uncontrolled local open fallback; it now opens from the existing URL-backed member sheet params or an explicit controlled prop, matching the members page open-button/sheet contract.
  Member import now has a dedicated `member-import-sheet-header.tsx` that owns the import title, upload/template/column-filter controls, and DnD column visibility popover; `member-import-sheet.tsx` keeps the sheet shell and import form body for the next content-router split.
  Member backfill baseline editing and high-impact backfill action sheets now use `memberBackfillSheetType` URL params instead of local sheet state, including row-specific opening-position review/apply/reverse ids and shared stage/share/dividend/draft/apply actions.
  Procurement, Foodstuff Purchase, and Project Financing workflow sheets now use URL-backed `nuqs` params instead of shadcn `SheetTrigger` local state, with row-specific request/cycle/application ids and close-on-success behavior for staff and member self-service actions.
  Support cases, payment receipts, and additional share applications now use URL-backed `nuqs` params instead of shadcn `SheetTrigger` local state, with row-specific case/receipt/application ids and close-on-success behavior across staff and member self-service actions.
  Remaining migration helpers no longer import shadcn `SheetTrigger`: quick fill and member backfill cell controls now use controlled shadcn sheets with normal buttons, and the server-rendered initial migration preview delegates action-sheet opening to a small client `MigrationActionSheet` wrapper.
  Procurement, Foodstuff Purchase, Project Financing, Support Cases, Payment Receipts, and Additional Share Applications no longer define page-local action sheet wrappers; their staff and member self-service actions now use dedicated URL-backed open-button/sheet/header/content files while preserving row-specific ids, wider receipt sheets, and existing server-action forms.
  Member backfill high-impact workflow actions now use a member-backfill-specific URL-param sheet wrapper with standard shadcn `Sheet` primitives, keeping member-backfill-specific params and small trigger sizing without the old generic workflow sheet bridge.
  Initial migration action sheets and member ledger backfill cell-control sheets now use controlled shadcn `Sheet` primitives through focused migration/backfill wrappers. The member ledger backfill controls were also renamed from `*Dialog` to `*Sheet`, leaving only `AlertDialog` confirmation gates in the stale dialog scan.
  Quick fill controls now use controlled shadcn sheet primitives with a normal button trigger, and the unused generic workflow action sheet component was removed after the final references were eliminated.
  The global sheets provider now mounts prop-free URL-driven workflow sheets for guarantor approvals and notification preferences, removing their page-local mounts and moving the dashboard closer to Midday's route-wide sheet provider model while leaving data-dependent sheets page-scoped.
  Settings sheets for cooperative profile, trust readiness, workspace roles, and operation profile now follow the same shell/header/content split: the `components/sheets/*-settings-sheet.tsx` files own only URL-state sheet shells, while dedicated `*-settings-sheet-header.tsx` and `*-settings-content.tsx` files own titles, descriptions, and form bodies.
  Member import now completes the same shell/header/content split: `member-import-sheet.tsx` owns only URL-backed sheet state, `member-import-sheet-header.tsx` owns import actions and column controls, and `member-import-content.tsx` owns the editable import grid, review gates, submit handling, and footer.
  Generic import batch sheets now follow the same file split: the route header imports `open-import-sheet.tsx`, `components/sheets/import-sheet.tsx` owns only URL-backed sheet state and width selection, `import-sheet-header.tsx` owns the title/description rules, `import-content.tsx` owns create/details/apply bodies, and `import-sheet-types.ts` centralizes the shared batch row shape.
  Global guarantor approval and notification preference sheets now keep headers and form bodies outside their sheet shells via `guarantor-approval-sheet-header.tsx`, `notification-preference-sheet-header.tsx`, and `notification-preference-content.tsx`, leaving the `components/sheets/*` files focused on URL-backed open/close state and Suspense boundaries.
  The post-create member backfill confirmation now follows the same split: `member-backfill-start-sheet.tsx` keeps only the sheet shell and close behavior, while `member-backfill-start-sheet-header.tsx` and `member-backfill-start-content.tsx` own the prompt copy, member summary, and actions.
  The member backfill baseline editor now follows the same shell/header/content split: `member-backfill-baseline-edit-sheet.tsx` keeps the trigger and URL-backed sheet shell, `member-backfill-baseline-edit-sheet-header.tsx` owns the edit copy, `member-backfill-baseline-edit-content.tsx` owns the shadcn form/mutation/refresh behavior, and `member-backfill-baseline-edit-types.ts` shares the member shape.
  Additional share application create/review workflow forms moved out of the page panel into `share-application-content.tsx`, leaving `share-applications-panel.tsx` focused on the policy summary, application lists, and URL-backed workflow sheet orchestration.
  Procurement workflow forms moved out of `procurement-requests-view.tsx` into `procurement-request-content.tsx`: staff request creation, member self-service requests, finance review, and purchase recording now own their form state, server actions, notifications, and refresh behavior outside the page view while the view keeps summary cards, request cards, and URL-backed sheet orchestration.
  Foodstuff Purchase workflow forms moved out of `food-purchase-view.tsx` into `food-purchase-content.tsx`: monthly fund release, staff member applications, member self-service applications, application review, cycle accounting, and accounting review now own their form state, server actions, notifications, and refresh behavior outside the page view.
  Project financing workflow forms moved out of `project-financing-requests-view.tsx` into `project-financing-content.tsx`: staff request creation, member self-service requests, finance review, and disbursement recording now own their form state, server actions, notifications, and refresh behavior outside the page view.
  Support case workflow forms moved out of `support-cases-view.tsx` into `support-case-content.tsx`: staff/member case creation, staff status updates, finance-adjustment review, and staff/member replies now own their form state, server actions, notifications, and refresh behavior outside the page views.
  Payment receipt workflow forms moved out of `payment-receipts-view.tsx` into `payment-receipt-content.tsx`: staff receipt staging, receipt review/allocation adjustment, staff support escalation, member receipt submission, and member support escalation now own their form state, server actions, notifications, and refresh behavior outside the page views.
  Member share self-service requests now open through the URL-backed share application workflow sheet; the member request form moved into `share-application-content.tsx` alongside the staff share application forms.
  Initial migration action sheet and adjustment form bodies moved out of `initial-migration-preview.tsx` into `initial-migration-action-content.tsx`: member backfill save/apply confirmations, migration finalization, no-legacy-loan review, legacy loan draft create/edit forms, and per-profit migration adjustment forms now live outside the preview orchestration file.
  Getting-started setup mode, operation-profile substeps, profit-season review, and finalization form bodies moved out of `getting-started-page-view.tsx` into `getting-started-content.tsx`, leaving the page view focused on step orchestration and footer navigation.
  Share history rows now use the shared Midday-style table runtime instead of the local static table: the shares finance route loads persisted share table settings and sort params, the header exposes column visibility, and the table supports sticky effective-date rows, draggable/resizable headers, virtual rows, saved column settings, and the existing URL-backed share edit sheet.
  Charge definition rows now use the shared Midday-style table runtime instead of the local static table: the charges finance route loads persisted charge table settings and sort params, the header exposes column visibility, and the table supports sticky charge rows, draggable/resizable headers, virtual rows, saved column settings, and the existing URL-backed charge update/edit sheet.
  Contribution ledger rows now use the shared Midday-style table runtime instead of the local static table: the contributions route loads persisted contribution table settings and sort params, the header exposes column visibility, and the table supports sticky member rows, draggable/resizable headers, virtual rows, saved column settings, and the existing contribution sheet workflows.
  Shares, charges, contributions, and business table loading states now use the shared core table skeleton instead of the older static table-state skeleton helper, so table fallbacks match the draggable/resizable runtime structure.
  Remaining table empty states now route through the shared core table empty-state component instead of the legacy table-state helper: notifications, membership approvals, charge library, monthly records, loan portfolio, loan requests, and contribution ledger empty states all share the same Midday-style table edge-state path, and the unused legacy table-state module was removed.
  Member backfill opening-position, historical share purchase, profit-season adjustment, dividend calculation, and draft-save form bodies moved out of `member-backfill-page-view.tsx` into `member-backfill-content.tsx`, leaving the page view to orchestrate steps, sheets, summaries, and navigation while submit-owning content lives outside the page surface.
  Member share self-service and staff additional-share workflows now follow the same open-button/sheet/header/content split: `member-shares-view.tsx` and `share-applications-panel.tsx` render summary/list surfaces only, `open-share-application-sheet.tsx` owns the URL-param triggers, and the dedicated share application sheet/header files own the request/review sheet shells.
  Procurement staff and member self-service workflows now follow the same open-button/sheet/header/content split: `procurement-requests-view.tsx` renders summaries and request cards only, `open-procurement-request-sheet.tsx` owns URL-param triggers, and `sheets/procurement-request-sheet.tsx` with `procurement-request-sheet-header.tsx` owns create, review, purchase, and self-service sheet routing.
  Foodstuff Purchase staff and member self-service workflows now follow the same open-button/sheet/header/content split: `food-purchase-view.tsx` renders summaries, cycles, and application cards only, `open-food-purchase-sheet.tsx` owns URL-param triggers, and `sheets/food-purchase-sheet.tsx` with `food-purchase-sheet-header.tsx` owns release, application, accounting, accounting review, request review, and member self-service sheet routing.
  Project financing staff and member self-service workflows now follow the same open-button/sheet/header/content split: `project-financing-requests-view.tsx` renders summaries and request cards only, `open-project-financing-sheet.tsx` owns URL-param triggers, and `sheets/project-financing-sheet.tsx` with `project-financing-sheet-header.tsx` owns create, review, disbursement, and self-service sheet routing.
  Support case staff and member self-service workflows now follow the same open-button/sheet/header/content split: `support-cases-view.tsx` renders summaries, case cards, message history, and finance approval status only, `open-support-case-sheet.tsx` owns URL-param triggers, and `sheets/support-case-sheet.tsx` with `support-case-sheet-header.tsx` owns create, update, reply, financial-adjustment review, member create, and member reply sheet routing.
  Payment receipt staff and member self-service workflows now follow the same open-button/sheet/header/content split: `payment-receipts-view.tsx` renders summaries, receipt cards, allocation summaries, and review notes only, `open-payment-receipt-sheet.tsx` owns URL-param triggers, and `sheets/payment-receipt-sheet.tsx` with `payment-receipt-sheet-header.tsx` owns staff staging, finance review, staff support escalation, member receipt submission, and member support escalation sheet routing.
  Finance business settings no longer render the profit policy form inline: the page now shows a read-only policy summary, opens the shared `TenantFinanceSettingsSheet` through a new `businessProfitPolicy` URL mode, and keeps the form body in `tenant-finance-settings-content.tsx`.
  Getting-started start-date, charge, profit-policy, and business-history form ownership moved from `getting-started-page-view.tsx` into `getting-started-content.tsx`, leaving the page view focused on wizard card orchestration.
  Monthly record automation summary naming was updated so the page view no longer looks like it owns an inline settings form; the actual settings edit remains in the monthly-record sheet content.
  Membership approval detail now delegates the final approval form card to `membership-approval-content.tsx`, leaving the dynamic route focused on data loading, applicant summary, and verification context.
  Member detail edit actions now use a URL-backed member-detail sheet instead of inline forms: portal access, commitment updates, KYC updates, document attachment, and document review open through `open-member-detail-sheet.tsx`, `sheets/member-detail-sheet.tsx`, `member-detail-sheet-header.tsx`, and `member-detail-content.tsx`.
  Share policy editing now opens through the existing URL-backed share sheet in `policy` mode. Finance share workspaces show the saved share model summary and mount `ShareSheet` at the workspace level, while the share history table can skip its internal sheet mount to avoid duplicate sheet owners.
  The business profit migration worksheet now owns its publish-allocation button locally instead of importing a worksheet-only action button from the large tenant finance forms module.
  The reports audit detail route now uses a dedicated Midday-style audit table runtime instead of stacked event cards: the route loads persisted `audit` table settings and sort params, hydrates a `reports.auditEvents` tRPC infinite query with cursor/page-size/sort/filter input, exposes column visibility, and renders activity events with draggable/resizable headers, virtual rows, and a sticky action column while preserving the existing audit filters and summary cards.
  Notifications delivery history now also uses a hydrated tRPC infinite query (`notifications.deliveryHistory`) instead of server-mapped table rows. The route seeds the first page for summary cards, while the table owns subsequent delivery-history fetching, filters, sort params, skeleton, empty state, draggable/resizable headers, virtual rows, and persisted notification table settings.
  Membership approvals now use a hydrated tRPC infinite query (`onboarding.membershipApprovals`) instead of route-passed table rows. The member onboarding request query supports cursor and sort inputs, the route seeds the first page for stats/table hydration, and the table owns filter/sort fetching, infinite scroll, skeleton, empty/error states, draggable/resizable headers, virtual rows, and persisted approval table settings.
  The operational charge library table now consumes a hydrated `charges.chargeLibrary` tRPC infinite query instead of receiving route-passed charge rows. The route still keeps server charge data for stats and history disclosure cards, while the table owns sort fetching, skeleton, empty/error states, infinite scroll, draggable/resizable headers, virtual rows, and persisted charge-library table settings.
  Contribution ledger rows now consume a hydrated `contributions.ledger` tRPC infinite query instead of route-passed ledger items. The contribution DB query supports cursor and sort inputs, the route seeds the first page for table hydration, and the table owns filters, sort params, infinite scroll, skeleton, empty/error states, draggable/resizable headers, virtual rows, and persisted contribution table settings.
  Loan request and loan portfolio tables now consume hydrated `loans.requests` and `loans.portfolio` tRPC infinite queries instead of route-passed table items. The new loans router wraps the existing loan DB lists without changing their shared consumers, supports member-scoped self-service views, and gives both tables sort params, infinite scroll, skeleton, empty/error states, draggable/resizable headers, virtual rows, and persisted loan table settings.
  Monthly record member rows now consume a hydrated `monthlyRecords.rows` tRPC infinite query instead of the table receiving selected-record rows directly. The route still loads selected-record detail for stats and the action sheet, while the table owns sort params, infinite scroll, skeleton, empty/error states, draggable/resizable headers, virtual rows, and persisted monthly-record table settings.
  Finance charge and share history tables now consume hydrated `charges.financeCharges` and `charges.financeShares` tRPC infinite queries instead of using their route-loaded rows as table data. The existing route rows remain as sheet context and no-database fallback data, while both tables own filter params, sort params, infinite scroll, skeleton, empty/no-results states, draggable/resizable headers, virtual rows, and persisted finance table settings.
  Import batch tables now consume a hydrated `imports.batches` tRPC infinite query instead of using route-loaded batches as table data. The import query supports cursor paging, section-scoped import type, status/search filters, sort params, and table hydration while keeping route-loaded batches available for overview stats and import sheet context.
  Dashboard navigation now keeps active nested settings/import groups expanded on direct route entry, adds explicit overview sublinks for finance setup and imports, and replaces generic support/procurement/food icons with more domain-specific lucide icons.
  Finance/import search filters now initialize their visible text input from URL `q` params, import batch filtering exposes the failed status, and report export date parsing now ignores invalid date query strings instead of passing invalid `Date` objects into CSV builders.
  Standalone member creation now uses a URL-backed `createMemberSheet` param instead of local open state for its migration overview trigger, while controlled guarantor creation keeps its existing URL-driven flow.
  Public member auth/signup status routes now share a Midday-style split `PublicAuthShell`: password reset request/confirm, member signup, signup verification, and awaiting-approval pages use the shared shell and standard card/button radius instead of one-off oversized public cards.
  A dashboard stale-style scan now leaves only safety confirmation `AlertDialog` usages from the old modal/table pattern set: remaining arbitrary large-radius form/card wrappers, negative tracking headings, and native/static table markers were normalized across member, finance, settings, import, signup-link, membership approval, member statement, and topbar surfaces.
  The `/domains` route now follows the same route/view split as other migrated dashboard pages: the server page only loads tenant domain data and delegates rendering to `domains-view.tsx`, which owns the domain stats, canonical-host summary, and hostname list.
  Import settings now follow the same route/view split: `settings/imports/imports-route.tsx` keeps search params, permissions, migration readiness, table settings, and tRPC hydration, while `imports-settings-view.tsx` owns the secondary menu, overview readiness cards, blocker summaries, import header, and import table handoff.
  Cooperative profile settings now also follows the route/view split: the route resolves profile labels/default values and permission state, while `profile-settings-view.tsx` owns the profile stats, detail cards, edit action, and profile settings sheet mount.
  Workspace roles, trust readiness, operation profile, notifications, member signup links, membership approvals list/detail, first-run onboarding, members, and the reports audit detail route now follow the same route/view split: their routes keep auth/search-param/data/hydration responsibilities, while dedicated view components own stats, cards, summary lists, sheet triggers, and table handoffs.
  Member import, standalone member create, and post-create backfill prompt sheet shells now live under `components/sheets` while their headers, content routers, and form contexts stay in dedicated ownership files, reducing legacy root/modal naming around member workflows.
  Member signup link, member backfill action, member backfill baseline edit, and initial migration action sheet shells also moved under `components/sheets`, leaving their domain folders focused on headers, content, tables, and workflow pages.
  Dashboard app/component class names no longer use Tailwind letter-spacing utilities (`tracking-*`), keeping page titles, labels, table helper text, and shared shells aligned with the zero-letter-spacing UI rule.
  Finance settings secondary navigation now uses a shared `financeMenuItems` source across overview, shares, charges, business, and profit-migration routes, reducing per-route drift during the Midday migration.
  Procurement, Foodstuff Purchase, Support, Payment Receipts, Project Financing, member share self-service, and guarantor approvals now follow the route/view split: route files load params/data only while dedicated page-view components own state-specific shells, empty states, and member/staff view handoff.
  Finance business settings now follows the same route/view split: the route resolves params, runtime, tenant name, and policy defaults, while `finance-business-settings-view.tsx` owns the visible policy summary, edit action, secondary menu, and tenant finance settings sheet.
  Member signup links, reports, trust readiness, and operation profile settings now delegate their access/runtime empty states to their view components, further reducing route-owned shell UI.
  Loans, repayments, operational charges, contributions, membership approvals list/detail, and audit report routes now also delegate runtime/access empty states to their page-view components, keeping routes focused on params, data loading, and tRPC hydration.
  Workspace roles, member detail, member backfill, and first-run onboarding routes now delegate their unavailable/access empty states to their page-view components, continuing the route/data versus view/shell split.
  Monthly records and getting-started routes now delegate their unavailable/access empty states to their page-view components as well; the sidebar route scan no longer finds route-level `WorkspacePageShell` or `WorkspaceEmptyState` usage.
  Finance share settings, charge settings, and business profit migration now delegate visible secondary menu/title/table-or-worksheet handoff to dedicated view components, leaving those finance route files focused on data mapping and hydration.
  Monthly records now delegates its year filter form to a focused `MonthlyRecordYearControl`, and public password reset/awaiting-approval routes delegate server-action form markup to shared public auth form components. Current route/page-view scans no longer find literal page-owned forms; remaining action hits are component props or section actions.
  The staff procurement workspace now uses a Midday-style procurement table runtime instead of request cards: the route loads persisted `procurement` table settings, the staff view exposes column visibility and create action through a procurement header, and `components/tables/procurement/*` owns TanStack columns, draggable/resizable headers, sticky request/action columns, virtual rows, row selection, empty state, and row-click review/purchase sheet routing while preserving existing URL-backed procurement sheets.
  The staff Foodstuff Purchase applications queue now uses a Midday-style table runtime instead of application cards: the route loads persisted `foodPurchase` table settings, the staff view exposes column visibility and application action through a Foodstuff Purchase header, and `components/tables/food-purchase/*` owns TanStack columns, draggable/resizable headers, sticky application/action columns, virtual rows, row selection, empty state, and row-click review sheet routing while preserving monthly cycle cards and existing URL-backed Foodstuff Purchase sheets.
  The staff project financing queue now uses a Midday-style table runtime instead of request cards: the route loads persisted `projectFinancing` table settings, the staff view exposes column visibility and create action through a project financing header, and `components/tables/project-financing/*` owns TanStack columns, draggable/resizable headers, sticky request/action columns, virtual rows, row selection, empty state, and row-click review/disbursement sheet routing while preserving existing URL-backed project financing sheets.
  The staff support case queue now uses a Midday-style table runtime instead of case cards: the route loads persisted `support` table settings, the staff view exposes column visibility and create action through a support header, and `components/tables/support/*` owns TanStack columns, draggable/resizable headers, sticky case/action columns, virtual rows, row selection, empty state, and row-click update sheet routing while preserving update, reply, and finance-adjustment review sheet actions.
  The staff payment receipts queue now uses a Midday-style table runtime instead of receipt cards: the route loads persisted `paymentReceipts` table settings, the staff view exposes column visibility and stage-receipt action through a payment receipt header, and `components/tables/payment-receipts/*` owns TanStack columns, draggable/resizable headers, sticky receipt/action columns, virtual rows, row selection, empty state, and row-click review/support sheet routing while preserving existing URL-backed receipt review/support sheets.
  Share application workflows now use a shared `shareApplications` table runtime across member self-service and finance share settings: persisted column settings, column visibility, sticky application columns, virtual rows, row selection, member-safe read-only history, and finance review actions replace the old application card lists.
  Member-facing procurement, Foodstuff Purchase, project financing, support, and payment receipt histories now reuse the same Midday-style table runtimes as their staff queues, with member create/request cards preserved as focused actions and support/payment row behavior switched to member-safe reply/support sheet modes.
  Payment receipt tables now use `trpc.paymentReceipts.list` for API-backed infinite loading and `trpc.paymentReceipts.get` for URL-selected sheet hydration, keeping staff receipt review/support and member receipt support sheets working even when rows come from filters or later pages.
  Procurement tables now use `trpc.procurement.list` for API-backed infinite loading, URL search/status filters, and `trpc.procurement.get` for URL-selected review/purchase sheet hydration, keeping staff and member-scoped procurement histories tenant/member safe as rows move beyond the server-loaded first batch.
  Foodstuff Purchase application tables now use `trpc.foodPurchase.list` for API-backed infinite loading, URL search/status filters, and `trpc.foodPurchase.get` for URL-selected application review sheet hydration, while cycle release/accounting context remains server-loaded for the non-table cycle workflow.
  Project financing tables now use `trpc.projectFinancing.list` for API-backed infinite loading, URL search/status filters, and `trpc.projectFinancing.get` for URL-selected review/disbursement sheet hydration, keeping staff and member-scoped project financing histories tenant/member safe as rows move beyond the server-loaded first batch.
  Share application tables now use `trpc.shareApplications.list` for API-backed infinite loading, dedicated share-application URL search/status filters, and `trpc.shareApplications.get` for URL-selected review sheet hydration, keeping finance and member self-service share histories tenant/member safe as rows move beyond the server-loaded first batch.
  Support tables now use `trpc.support.list` for API-backed infinite loading, URL search/status/priority filters, and `trpc.support.get` for URL-selected update/reply/financial-adjustment sheet hydration, keeping staff and member-scoped support histories tenant/member safe as rows move beyond the server-loaded first batch.
  Remaining follow-up slices: broader completion audit, reporting table contract hardening beyond audit, and any newly discovered page-specific parity gaps.

### Port Dashboard Tables To Midday Core Parity

- Priority: Medium
- Description: Track plan in `brain/plans/2026-06-30-feature-midday-dashboard-table-core-parity.md`.
- Related Feature: Midday Dashboard Table Core Parity
- Status: In Progress
- Plan Status: In Progress
- Plan File: brain/plans/2026-06-30-feature-midday-dashboard-table-core-parity.md
- Created Date: 2026-06-30
- Progress: Phases 0-4 are done, the members pilot is complete, audit event history, notification delivery history, membership approvals, the operational charge library, contribution ledger, loan requests, loan portfolio, monthly record member rows, finance charges, finance share history, and import batches now use hydrated infinite-query table contracts, and the legacy `components/dashboard/static-table.tsx` compatibility module has been removed after consumers moved to Midday-style table runtimes or intentional surface-list layouts. Phase 5 data-contract hardening and any remaining reporting/list-heavy route coverage are still in progress.

### School-Clerk-Style Notification Email And Job Implementation

- Priority: Medium
- Description: Track plan in `brain/plans/2026-06-23-feature-school-clerk-style-notification-email-and-job-implementation.md`.
- Related Feature: School-Clerk-Style Notification Email And Job Implementation
- Status: In Progress
- Plan Status: In Progress
- Plan File: brain/plans/2026-06-23-feature-school-clerk-style-notification-email-and-job-implementation.md
- Created Date: 2026-06-23
# Cross-product QA email and cleanup

- Implementation is integrated in Halaalvest and ported to EwaTrade, PlotKeys, SchoolClerk, and afterservice.
- Remaining rollout work is schema deployment, secure environment propagation, canary delivery, and the first operator-reviewed purge preview.
