# Decide Repeatable Local QA Environment Strategy

Status: resolved
Type: `wayfinder:task`
Parent map: [Wayfinder: Minna Trust Cooperative Onboarding QA](../MAP.md)

## Question

What local test environment and reset strategy should QA use for this cooperative onboarding scenario so the run is repeatable without corrupting existing development data?

The answer should decide whether to reuse the currently opened tenant, create a fresh tenant, reset local data, seed specific records, or use browser-only setup, and should define how email/onboarding tokens will be captured safely.

## Resolution

Resolved in [ENVIRONMENT.md](../ENVIRONMENT.md).

The local strategy uses `bun run dev --local --filter dashboard marketing`, portless URLs, local-only evidence capture, and an explicit choice between the currently opened Minna Trust tenant and a fresh local tenant. Production mutation is out of scope.

## Comments

> *This was generated by AI during triage.*
Please decide whether QA should reuse the current Minna Trust tenant or create a fresh local tenant. The resolution should include reset/setup commands, the exact portless URLs, how to capture onboarding/password email tokens locally, and how to avoid mutating production or unrelated dev data.
