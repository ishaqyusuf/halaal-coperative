{
  "name": "halaal-coperative",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "db:down": "docker compose down",
    "db:generate": "DATABASE_URL=postgres://postgres:postgres@localhost:5432/halalcoop bun run --cwd packages/db db:generate",
    "db:logs": "docker compose logs -f postgres",
    "db:migrate": "DATABASE_URL=postgres://postgres:postgres@localhost:5432/halalcoop bun run --cwd packages/db db:migrate",
    "db:migrate:deploy": "DATABASE_URL=postgres://postgres:postgres@localhost:5432/halalcoop bun run --cwd packages/db db:migrate:deploy",
    "db:studio": "DATABASE_URL=postgres://postgres:postgres@localhost:5432/halalcoop bun run --cwd packages/db db:studio",
    "db:up": "docker compose up -d postgres",
    "dev": "turbo dev --parallel",
    "dev:api": "turbo dev --filter=@halalcoop/api",
    "dev:web": "turbo dev --filter=@halalcoop/web",
    "dev:dashboard": "turbo dev --filter=@halalcoop/dashboard",
    "lint": "turbo lint",
    "format": "biome format --write .",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "turbo": "^2.5.0",
    "typescript": "^5.9.0"
  }
}
