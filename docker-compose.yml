services:
  postgres:
    image: postgres:16-alpine
    container_name: halaalvest-postgres
    restart: unless-stopped
    ports:
      - "${DB_HOST_PORT:?HALAALVEST_DATABASE_URL must include a local PostgreSQL port}:5432"
    environment:
      POSTGRES_DB: "${DB_NAME:?HALAALVEST_DATABASE_URL must include a database name}"
      POSTGRES_USER: "${DB_USER:?HALAALVEST_DATABASE_URL must include a user}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:?HALAALVEST_DATABASE_URL must include a password}"
    volumes:
      - halaalvest-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  halaalvest-postgres-data:
