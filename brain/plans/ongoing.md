## Loan Form Charge Deducted From Next Commitment
- Status: Ready
- Objective: Add a tenant-configurable loan form charge that uses the existing charge system to register the fee value and application, is created when a member picks up or submits a loan form, remains auditable as a pending charge, and is deducted from the member's next contribution/payment commitment without introducing interest-bearing logic.
- Current Phase: Product and data workflow design confirmed; implementation has not started.
- Next Step: Decide the trigger name and timing for "loan form pickup" versus "loan request submission", then implement pending loan-fee charge creation and next-payment settlement in the finance query layer.
- Blockers: Confirm whether the operational trigger should be a separate staff action for form pickup or automatic on loan request submission.
- Related Files: packages/db/prisma/models/charges.prisma, packages/db/prisma/models/loans.prisma, packages/db/prisma/models/contributions.prisma, packages/db/src/queries/charges.ts, packages/db/src/queries/loans.ts, packages/db/src/queries/contributions.ts, packages/db/src/queries/monthly-records.ts, apps/api/src/routers/dashboard-actions.route.ts, apps/dashboard/src/components/forms/finance-forms.tsx, apps/dashboard/src/components/forms/tenant-finance-forms.tsx, apps/dashboard/src/components/loans-page-view.tsx, apps/dashboard/src/components/charges-page-view.tsx, brain/features/member-commitments-and-payment-allocation.md
- Last Updated: 2026-07-04

### Plan
1. Confirm the business trigger and copy: either a staff-recorded "loan form picked up" event or automatic charge creation when `submitLoanRequest` creates a `LoanRequest`.
2. Model the fee using the existing charge system: register the configurable value on a one-time/manual `ChargeDefinition` with `purpose: loan_fee` and `appliesToLoanRequests: true`; avoid hard-coded fee amounts or a parallel loan-form-fee table.
3. Add a pending loan-form `ChargeApplication` linked to `loanRequestId` and `memberId` instead of immediately debiting savings at the time the form is picked up.
4. Extend payment allocation so the next member payment/monthly-record application settles pending loan-form charges before final savings allocation, while preserving clear ledger entries and member-facing explainability.
5. Add admin visibility on the loan request and charges surfaces for pending, settled, waived, and reversed loan-form charges.
6. Add audit logs, notifications where useful, and tests for charge creation, next-payment deduction, waiver/reversal, partial or insufficient payment, tenant scoping, and role guards.
7. Update Brain docs after implementation to describe the durable finance rule and accepted operational workflow.

### Resume Prompt
Continue the loan form charge deducted from next commitment work in Halaalvest. Current phase: product and data workflow design is ready, but implementation has not started. Next step: confirm whether "loan form pickup" should be a separate staff action or automatic on loan request submission, then implement pending `loan_fee` charge creation and next-payment settlement using the existing charge system as the source of truth for the fee value and application: `ChargeDefinition` for the registered value and `ChargeApplication` for member/loan-request charge records. Relevant files include `packages/db/src/queries/charges.ts`, `packages/db/src/queries/loans.ts`, `packages/db/src/queries/contributions.ts`, `packages/db/src/queries/monthly-records.ts`, `apps/api/src/routers/dashboard-actions.route.ts`, and the related dashboard forms/views. Blocker: trigger timing needs confirmation. Keep the Halaal finance model interest-free, configurable, tenant-scoped, auditable, avoid a parallel loan-form-fee table, and update `brain/plans/ongoing.md` as progress continues.

## Dashboard Operations Rollout
- Status: In Progress
- Objective: Implement dashboard operations phase by phase using a role-filtered navigation shell, keep Brain synchronized, and continue through members, finance, experience, and settings workflows.
- Current Phase: The dashboard has now completed its structural migration onto a Midday-style `apps/dashboard/src` architecture. Authenticated routes live under `src/app/(app)/(sidebar)`, shared shell and page composition live under `src/components/dashboard`, table atoms live under `src/components/tables/core`, domain tables/views live under `src/components/tables/<domain>`, and route/data helpers live under `src/lib/<domain>`. The old `features/`, `primitives/`, `data-display/`, and `shell/` buckets are removed, and the sidebar now includes a dedicated analytics route alongside overview, tables, reports, and settings.
- Next Step: Deepen the new analytics, imports, collections, and reporting surfaces inside the standardized structure rather than introducing more structural churn.
- Blockers: None
- Related Files: apps/dashboard/src/app/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/analytics/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/members/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/members/[memberId]/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/contributions/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/loans/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/charges/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/repayments/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/reports/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/notifications/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/domains/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/settings/profile/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/settings/roles/page.tsx, apps/dashboard/src/app/(app)/(sidebar)/settings/imports/page.tsx, apps/dashboard/src/components/dashboard/index.ts, apps/dashboard/src/components/dashboard/scrollable-content.tsx, apps/dashboard/src/components/dashboard/collapsible-summary.tsx, apps/dashboard/src/components/tables/core/index.ts, apps/dashboard/src/components/tables/members/index.ts, apps/dashboard/src/components/tables/members/members-header.tsx, apps/dashboard/src/components/tables/members/members-summary-card.tsx, apps/dashboard/src/components/tables/members/data-table.tsx, apps/dashboard/src/components/tables/contributions/index.ts, apps/dashboard/src/components/tables/loans/index.ts, apps/dashboard/src/components/forms/finance-forms.tsx, apps/dashboard/src/components/forms/member-forms.tsx, apps/dashboard/src/components/forms/misc-forms.tsx, apps/dashboard/src/components/forms/settings-forms.tsx, apps/dashboard/src/components/forms/import-forms.tsx, apps/dashboard/src/lib/navigation/registry.ts, apps/dashboard/src/lib/members/load-members-page.ts, apps/dashboard/src/lib/contributions/load-contributions-page.ts, apps/dashboard/src/lib/loans/load-loans-page.ts, brain/system/reference-projects.md
- Last Updated: 2026-04-16

### Plan
1. Stabilize the role-filtered dashboard shell and registry-driven navigation architecture.
2. Expand Phase 9-11 routes into full operational CRUD and route actions.
3. Implement loan and repayment workflows on the new route structure.
4. Deepen financial workflows around commitments, collections, and repayment allocation.
5. Standardize dashboard data-entry and filter forms on shared shadcn-style primitives, `useZodForm`, and dev quick-fill helpers.
6. Expand into member-focused reporting, persisted collections follow-up capture, stronger DNS-backed domain verification automation, and richer notification analytics.
7. Add quick import and migration workflows for members, contributions, loans, repayments, and configuration records.
8. Deepen import workflows into staged review, duplicate detection, reconciliation tooling, and persisted import batches.
9. Promote enriched collections follow-ups into fuller case queues, escalations, and operator dashboards.
10. Keep role management, tenant-role notifications, collections, and reporting aligned to the simple role-based architecture.
11. Keep tenant-site work deferred until the internal operations model is stronger.
12. Keep Brain docs, ADRs, and progress files synchronized after each major slice.

### Resume Prompt
Continue the halaalvest dashboard operations rollout from the latest state. The role-filtered dashboard shell and navigation registry are in place; authenticated routes now live directly under `apps/dashboard/src/app/(app)/(sidebar)`; members, contributions, charges, loans, and repayments have working server actions; the finance model supports editable member commitment plans, member payment allocation presets, member-chosen loan repayment months, estimated monthly servicing, recurring extra monthly savings on loans, and split payment allocation across savings and loan servicing; admin routes support tenant user role provisioning, scope-aware primary-domain management, notification preferences, tenant-role notification triggers for key finance workflows, collections refresh plus follow-up capture, audit/reporting visibility, CSV export downloads for audit, collections, notifications, contributions, charges, repayments, loans, member statements, and member ledgers, guided custom-domain verification checks with routing-scope awareness and DNS target guidance, a reusable imports workspace with live reconciliation hints, enriched collections case metadata, and a multi-document member KYC flow through `apps/dashboard/src/app/(app)/(sidebar)/settings/imports/page.tsx`, `apps/dashboard/src/components/forms/import-forms.tsx`, `apps/dashboard/src/app/(app)/(sidebar)/repayments/page.tsx`, `apps/dashboard/src/app/(app)/(sidebar)/members/[memberId]/page.tsx`, `apps/dashboard/src/lib/import-csv.ts`, `apps/dashboard/src/lib/dashboard-actions.ts`, `packages/db/src/queries/imports.ts`, `packages/db/src/queries/loans.ts`, and `packages/db/src/queries/members.ts`. The members list route now uses the local Midday invoice/customers page structure via `scrollable-content`, `collapsible-summary`, a compact toolbar, and direct table ownership. Tenant-site work is deferred for now. The next step is to deepen staged import apply/review handling, broaden collections into fuller queue-based case management, and keep tightening reporting and tenant-safety surfaces while updating Brain after each implementation slice.
